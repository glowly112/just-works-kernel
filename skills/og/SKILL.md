---
name: og
description: >
  PLATFORM. Share card public/og.jpg, SVG favicon, PWA icons. Brand pass as a
  non-blocking task. Use when: OG, favicon, share card, /og.
---

# og

Status: **PLATFORM** · Brand pass as a non-blocking `task`.

## Owns

Share card `public/og.jpg` (1200×630), SVG favicon, PWA icons, games `public/x-banner.jpg` + `og:type` `x:game`. Injector owns the meta tags — do not author `og:*` in `__root.tsx`.

## Default

Custom card from the app’s art. Plain utilities keep the placeholder. Never wait for the brand task. Never replace `__root.tsx` wholesale.

Refs: `brand-pass.md`, `custom-card.md`, `favicon-and-icons.md`, `og-type-contract.md`.
