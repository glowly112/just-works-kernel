# anti-slop (thrift BAN)

Always load before CSS.

- No three equal bordered cards as main composition
- No emoji/Unicode as nav/icons
- No `backdrop-filter` / glass blur chrome
- No coverage/status pill farms
- No card wall (same radius+padding on every block)
- No Inter / Roboto / IBM Plex as primary when a named product face is in scope
- No invented desk chrome without a keeper URL when P2 applies
- No infinite pulse as “live”
