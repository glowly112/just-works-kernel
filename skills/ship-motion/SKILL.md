---
name: ship-motion
description: >
  After verify-done, keep git / Vercel / STATE moving. Not on-screen animation
  (that is ui-marks). Use when: commit, push, Vercel, ship, /ship-motion.
---

# ship-motion

**Not on-screen animation.** Page-turn, route-in, icons, pulse = **ui-marks**. This skill is git / Vercel / STATE.

1. Commit changed files, push if remote
2. Vercel latest deploy if `.vercel` or they asked live
3. STATE.md
4. Notion only if STATE/AGENTS already has the page link
