---
name: vibe-stack
description: >
  Index for the Grok + Notion skill chain. Load just-works first. Not Compound
  Engineering. Use when: skill stack, load order, vibe to production, /vibe-stack.
---

# Skill stack — vibe to production

**Load `just-works` first** for any accepted work. The short path in `skills/just-works/SKILL.md` is SOT. Kernel does not win on conflict. A FEATURE.md / FEATURE.yaml stamp is not a load. ui-thrift stays the pixel path. Next gate only.

Index. How the Grok + Notion skills chain. Not Compound Engineering. Not Superpowers-the-plugin.

## Load order (do not mega-merge)

1. **read-me** — ordinary English / slang / screenshots → restated job
2. **repo-memory** — read STATE / CONCEPTS / LEARNINGS
3. **iOS?** → **ios-26** — system chrome + the product’s thrift charm. Then ui-thrift for pixels.
4. **Pixels?** → **ui-thrift** (uk-copy when there are words). Leaves only after 2 fails.
4b. **Marks?** → **ui-marks** — icons, SVG, portraits, small CSS motion. Skip if none asked.
5. **Behaviour?** → **feature-plan** → accept FEATURE.md (include **SOT** if an existing system)
6. **tdd-one** — one RED test per planned step that can run
7. Build the step
8. **hammer** — 5–8 glare cases
9. **verify-done** — fresh command this turn
10. Bug survives → **debug-once** (max 2 patches)
11. Ship → update repo-memory

## Token rule

Load only the next gate. Do not preload the whole stack.

## After ship

- **unlazy** — no stubs; first-paint
- **ship-motion** — commit, push, Vercel, STATE. Not on-screen animation.

## Packs

- Product path — this page + just-works
- pack-store — research → fit → list → prototype-to-testflight → ios-publish
- pack-platform
- pack-games
