---
name: tdd-one
description: >
  One failing test, watch it fail for the right reason, then the smallest code
  that passes. Default when behaviour is runnable. Exempt ui-thrift pixel-only.
  Use when: tdd, test first, /tdd-one.
---

# tdd-one

Status: **DEFAULT when behaviour is runnable in a test runner**

## Job

One failing test, watch it fail for the right reason, then the smallest code that passes. No 40-test generator. No prod code first.

Exempt ui-thrift pixel-only work.
