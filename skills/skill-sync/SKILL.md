---
name: Skill sync
description: >-
  Use this whenever a skill is written or rewritten. Same turn: local SKILL.md,
  matching Notion wiki page, GitHub glowly112/works, and just-works-kernel.
  The short Just works path is SOT. Kernel does not win on conflict.
---
Three copies plus the kernel that Cloud Agents curl. Same turn. Fail if any is missing.

The **short Just works path** is SOT. Kernel does **not** win on conflict. A 12-stage FEATURE packet is not this path.

## Where

Local skill write; Notion `Skill — <slug>`; glowly112/works `.grok/skills/<slug>/SKILL.md`; this kernel `skills/<slug>/SKILL.md` when the slug exists here.

Skills stay product-agnostic.
