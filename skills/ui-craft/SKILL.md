---
name: ui-craft
description: >
  Escalate-only. Not the default UI load path. Load ui-thrift first. Use after
  two thrift fails or an explicit /ui-craft ask. WCAG 2.2 AA + NN/g + anti-slop
  + SIGN uniqueness for web/mobile-web HTML/CSS. Not native iOS/Android.
---

# ui-craft (escalate-only)

**Escalate-only.** Not the default UI load path. Load **ui-thrift** first.

Status: **LEAF**

## Job

WCAG 2.2 AA + NN/g floor, anti-slop, SIGN uniqueness for web / mobile-web HTML/CSS. Not native iOS/Android.

## When

User names `/ui-craft`, or ui-thrift has failed twice on craft/slop/silhouette.

## Do not

Lead the session. Blend peer shells. Skip the design memo / SKELETON.
